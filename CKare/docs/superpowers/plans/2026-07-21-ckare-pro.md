# CKare Pro Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the caregiver/professional mobile app and corresponding standalone web experience.

**Architecture:** Use an independent Expo Router app importing shared policy and design packages. Build the web artifact as one self-contained HTML file with embedded CSS and JavaScript so GitHub Pages and direct-file previews work without a build step.

**Tech Stack:** Expo 55, React Native 0.83, Expo Router, TypeScript, standalone HTML/CSS/JavaScript.

## Global Constraints

- Primary navigation is Home, Learn, Work, Earnings, Profile.
- Activation is service- and jurisdiction-specific.
- Course completion is separate from certificate, license, payer, and marketplace states.
- Earnings dates and insurance payment are estimates, not guarantees.
- Preserve unrelated existing worktree changes.

---

### Task 1: CKare Pro Expo shell

**Files:**
- Create: `apps/ckare-pro/package.json`
- Create: `apps/ckare-pro/app.json`
- Create: `apps/ckare-pro/eas.json`
- Create: `apps/ckare-pro/tsconfig.json`
- Create: `apps/ckare-pro/app/_layout.tsx`
- Create: `apps/ckare-pro/app/(tabs)/_layout.tsx`
- Create: `apps/ckare-pro/src/components/Screen.tsx`
- Create: `apps/ckare-pro/src/components/StatusPill.tsx`
- Create: `apps/ckare-pro/src/data/demo.ts`
- Create: `tests/pro-contract.test.mjs`

**Interfaces:**
- Consumes: `@ckare/core`, `@ckare/design`.
- Produces: five routable tabs and reusable accessible screen/status components.

- [ ] **Step 1: Write failing configuration tests**

Assert app name `CKare Pro`, slug `ckare-pro`, scheme `ckarepro`, iOS bundle ID and Android package `com.ckare.pro`, and the five route files.

- [ ] **Step 2: Verify failure**

Run: `node --test tests/pro-contract.test.mjs`  
Expected: FAIL because the app files are absent.

- [ ] **Step 3: Implement app shell**

Configure typed routes, dark/light theme, safe-area screens, visible labels, and native tab icons with no protected-health-data permissions.

- [ ] **Step 4: Verify**

Run: `node --test tests/pro-contract.test.mjs`  
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/ckare-pro tests/pro-contract.test.mjs
git commit -m "feat: scaffold CKare Pro mobile app"
```

### Task 2: CKare Pro feature screens

**Files:**
- Create: `apps/ckare-pro/app/(tabs)/index.tsx`
- Create: `apps/ckare-pro/app/(tabs)/learn.tsx`
- Create: `apps/ckare-pro/app/(tabs)/work.tsx`
- Create: `apps/ckare-pro/app/(tabs)/earnings.tsx`
- Create: `apps/ckare-pro/app/(tabs)/profile.tsx`
- Modify: `apps/ckare-pro/src/data/demo.ts`
- Modify: `tests/pro-contract.test.mjs`

**Interfaces:**
- Produces: activation summary, qualification path, booking detail, documentation checklist, claim timeline, and credential profile.

- [ ] **Step 1: Add failing content and safety tests**

Assert screen source contains the required primary actions, explicit license/course distinction, authorization state, non-guaranteed payout language, and emergency boundary.

- [ ] **Step 2: Verify failure**

Run: `node --test tests/pro-contract.test.mjs`  
Expected: FAIL for missing screens.

- [ ] **Step 3: Implement all five screens**

Use typed fictional data, reusable status components, minimum 44-point controls, text scaling, and no color-only statuses.

- [ ] **Step 4: Verify**

Run: `node --test tests/pro-contract.test.mjs`  
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/ckare-pro tests/pro-contract.test.mjs
git commit -m "feat: build CKare Pro workflows"
```

### Task 3: CKare Pro standalone web experience

**Files:**
- Create: `web/ckare-pro.html`
- Create: `tests/web-pro.test.mjs`

**Interfaces:**
- Produces: direct-open responsive HTML with tab navigation, animated activation path, workflow dialogs, and accessible status announcements.

- [ ] **Step 1: Write failing HTML contract tests**

Assert one `main`, five named navigation controls, viewport metadata, skip link, reduced-motion CSS, responsive breakpoint, and required compliance copy.

- [ ] **Step 2: Verify failure**

Run: `node --test tests/web-pro.test.mjs`  
Expected: FAIL because the HTML file does not exist.

- [ ] **Step 3: Implement the self-contained experience**

Embed Quiet Confidence CSS and dependency-free JavaScript. Use semantic buttons, focus management, local tab state, demo-only workflow transitions, and fictional data.

- [ ] **Step 4: Verify**

Run: `node --test tests/web-pro.test.mjs`  
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add web/ckare-pro.html tests/web-pro.test.mjs
git commit -m "feat: add CKare Pro web experience"
```

